Just some tests for TCP

1)Edit address, port in client.erl and query string

2)Compile it:
 erlc *.erl

3)Run erl

$erl

Inside it type:
1) client:start().

It create 100 parallel endless loops with asks.

If need more, just repeat again client:start() in erl and join next 100 of loops
with asks.

